apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      run: my-pod
  template:
    metadata:
      labels:
        run: my-pod
    spec:
      containers:
      - name: my-pod
        image: underwaterhouse/go-api

